git init[initialise git]
git status[monitor untracked changes ]
git add -add[add all changes to staging area]
git commit -m "ur msg"
command for adding local repository to remote
>git push
exact copy from remote remote
>git clone
changes from remote remote
>git pull
  
  add and push to path
>git remote add origin path
>git push origin master
to check name of path
>git remote -v


exact copy
clone

changes
pull
list all branches
>git branch
